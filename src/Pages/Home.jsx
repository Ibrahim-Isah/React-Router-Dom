const Home = () => {
	// useEffect(() => {
	//     window.scroll(0,0)
	//     return () => {
	//         cleanup
	//     }
	// }, [input]) You can use this so that the page that loads load from the start of the screen
	return <h1>Home</h1>;
};

export default Home;
